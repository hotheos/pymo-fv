# (Movido) — Manejo de llaves de API

> Este documento quedó obsoleto. La decisión cambió a un modelo basado en
> **token (JWT)** sin API key en el cliente, tras aclarar que el puente Pymo
> (de nuestra propiedad) es quien valida el acceso a Mekano.
>
> **Ver el documento vigente:** [ARQUITECTURA_SEGURIDAD.md](./ARQUITECTURA_SEGURIDAD.md)

(Se puede borrar este archivo; se deja solo porque el entorno actual no permite eliminarlo.)
